# 服务器配置
PORT=3000
NODE_ENV=production

# MySQL 配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=receipt_admin
DB_PASSWORD=your_mysql_password
DB_NAME=receipt_manager

# JWT 密钥
JWT_SECRET=change_this_to_a_random_string_at_least_32_chars

# 前端地址（用于 CORS）
CLIENT_URL=https://career.aixiaolv.icu

# 文件上传大小限制 (MB)
MAX_UPLOAD_SIZE=10
